/// Bumped with every fix/feature shipped — shown briefly at app start (see
/// BuildBanner) so it's immediately obvious on the device whether a rebuild
/// actually picked up the latest change instead of silently running stale
/// code.
class BuildInfo {
  static const label =
      'Fix: Preis-Auflösung überschreibt Wishlist nicht mehr geräteübergreifend';
}
